(function() {
	laydate({
        elem: '#endTime',
		istime :true,
		format: 'YYYY-MM-DD hh:mm:ss'
    });
	laydate({
        elem: '#startTime',
		istime :true,
		format: 'YYYY-MM-DD hh:mm:ss'
	});
})();