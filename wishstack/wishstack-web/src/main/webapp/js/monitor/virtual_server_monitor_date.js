(function() {
	laydate({
        elem: '#endTimes',
		istime :true,
		format: 'YYYY-MM-DD hh:mm:ss'
    });
	laydate({
        elem: '#startTimes',
		istime :true,
		format: 'YYYY-MM-DD hh:mm:ss'
	});
})();